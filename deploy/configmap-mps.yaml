apiVersion: v1
kind: ConfigMap
metadata:
  name: nvidia-device-plugin-config
  namespace: nvidia-device-plugin
data:
  mps-100: |
    version: v1
    flags:
      migStrategy: none
      mpsMemoryLimiting: true
      plugin:
        passDeviceSpecs: true
        deviceListStrategy:
          - cdi-annotations
        deviceIDStrategy: uuid
    resources:
      gpus:
        - pattern: "*"
          name: gpu
    sharing:
      mps:
        renameByDefault: false
        failRequestsGreaterThanOne: false
        resources:
          - name: nvidia.com/gpu
            replicas: 100
            devices: "all"
